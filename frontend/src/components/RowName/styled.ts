import styled from 'styled-components';

export const RowNameStyled = styled.div`
    font-size: 16px;
    font-weight: bold;
    color: #333;
    margin-left: 10px;
    text-decoration: none;
    transition: color 0.1s ease;
    text-decoration: none;

    &:hover {
        color: #007bff;
        cursor: pointer;
    }
`;
